.DS_Store*
.clj-kondo
.cpcache
.direnv
.lsp
.portal
.vscode
build
flake-profile*

# Reflection scratch (use `bb reflect:dump > raw.edn` to materialise)
raw.edn
raw-reflection.edn

# Project documentation
CLAUDE.md
WORKPLAN.md
AGENTS.md
docs/plans/

# Allium spec drafts (work in progress, not source-controlled yet)
*.allium

# direnv local overrides
.envrc.bak

# Maven
target
java/target/
*.jar

# GraalVM build symlink
graal-home

# Fetched build JDKs (mx fetch-jdk output — the JVMCI labsjdk used to build the
# graal submodule; large, machine-local, not source). Set JAVA_HOME here.
.jdks/

# npm
node_modules/
dist/
js/src/
*.tgz
package-lock.json

# Build outputs (allow if committed)
# *.wasm
# *.wat
# *.js (from WASM)

# Auto-generated from registry.edn — regenerated by `bb gen:all` (mvn wires
# `bb gen:api`; npm build:js wires `bb gen:dts`; bb test wires `bb gen:tests`).
/registry.edn
/java/src/main/java/net/willcohen/wasmts/API_Generated.java
/test/clj/wasmts/differential/generated_test.clj
/types/wasmts.d.ts